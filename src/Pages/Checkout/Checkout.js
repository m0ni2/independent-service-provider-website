import React from 'react';
import { Container } from 'react-bootstrap';

const Checkout = () => {
    return (
        <Container className='bg-light d-flex justify-content-center align-items-center' style={{ height: 'calc(100vh - 197px)' }} fluid>
            <h2 style={{ fontSize: '5rem' }}>Checkout</h2>
        </Container>
    );
};

export default Checkout;